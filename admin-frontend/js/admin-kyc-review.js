(() => {
  const API_BASE = (location.hostname === "localhost" || location.hostname === "127.0.0.1") ? "http://127.0.0.1:8000" : (location.hostname === "bethel-api.onrender.com" || location.hostname === "api.betheltradingtechnologies.com") ? location.origin : "https://bethel-api.onrender.com";
  const state = { objectUrls: [] };
  const esc = value => String(value ?? "").replace(/[&<>"']/g, ch => ({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[ch]));
  function token(){return localStorage.getItem("bethel_access_token")}
  async function reviewRequest(path,options={}){const headers={Accept:"application/json",...(options.headers||{})};const accessToken=token();if(accessToken)headers.Authorization=`Bearer ${accessToken}`;if(options.body&&!headers["Content-Type"])headers["Content-Type"]="application/json";const response=await fetch(API_BASE+path,{credentials:"include",...options,headers});const type=response.headers.get("content-type")||"";const data=type.includes("application/json")?await response.json():await response.text();if(!response.ok){const detail=data&&typeof data==="object"?data.detail:data;throw new Error(typeof detail==="string"?detail:JSON.stringify(detail||`API error ${response.status}`))}return data}
  function ensureStyles(){if(document.getElementById("native-kyc-review-styles"))return;const style=document.createElement("style");style.id="native-kyc-review-styles";style.textContent=`.kyc-review-button{background:#0ea5e9!important;color:#fff!important;border-color:#38bdf8!important;font-weight:700}.kyc-direct-disabled{opacity:.42!important;pointer-events:none!important}.kyc-modal-backdrop{position:fixed;inset:0;background:rgba(2,6,23,.86);z-index:10000;display:flex;align-items:flex-start;justify-content:center;padding:3vh 18px;overflow:auto}.kyc-modal{width:min(1120px,100%);background:#071426;border:1px solid #1e3a5f;border-radius:18px;box-shadow:0 30px 100px rgba(0,0,0,.55);color:#e5eefb;overflow:hidden}.kyc-modal header{position:sticky;top:0;z-index:2;background:#071426f2;display:flex;justify-content:space-between;gap:20px;align-items:center;padding:20px 24px;border-bottom:1px solid #1e3a5f}.kyc-modal h2,.kyc-modal h3{margin:0}.kyc-modal main{padding:22px 24px 28px}.kyc-close{background:#172554!important;color:#fff!important}.kyc-warning{padding:14px 16px;border:1px solid #854d0e;background:#422006;color:#fde68a;border-radius:12px;margin-bottom:18px;font-weight:600}.kyc-assist{padding:16px;border:1px solid #2563eb;background:#0b2146;border-radius:14px;margin:0 0 22px}.kyc-assist-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(180px,1fr));gap:10px;margin-top:12px}.kyc-review-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(230px,1fr));gap:12px;margin:14px 0 22px}.kyc-review-card{background:#0b1d33;border:1px solid #1e3a5f;border-radius:12px;padding:13px}.kyc-review-card small{display:block;color:#93a4b8;margin-bottom:5px}.kyc-checks{width:100%;border-collapse:collapse;margin-top:10px}.kyc-checks th,.kyc-checks td{text-align:left;padding:10px;border-bottom:1px solid #1e3a5f;vertical-align:top}.kyc-check-passed{color:#34d399}.kyc-check-failed{color:#fb7185}.kyc-check-review,.kyc-check-not_available{color:#fbbf24}.kyc-evidence-toolbar{display:flex;gap:10px;align-items:center;flex-wrap:wrap;margin:16px 0}.kyc-evidence-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(280px,1fr));gap:16px}.kyc-evidence-item{background:#0b1d33;border:1px solid #1e3a5f;border-radius:12px;padding:12px}.kyc-evidence-item img,.kyc-evidence-item iframe{width:100%;height:360px;object-fit:contain;background:#020617;border:0;border-radius:8px}.kyc-decision{margin-top:24px;padding-top:20px;border-top:1px solid #1e3a5f}.kyc-decision textarea{width:100%;min-height:90px;background:#020617;color:#e5eefb;border:1px solid #334155;border-radius:10px;padding:10px;margin:10px 0 14px}.kyc-attest{display:flex;gap:10px;align-items:flex-start;margin:14px 0}.kyc-actions{display:flex;gap:12px;flex-wrap:wrap}.kyc-approve{background:#059669!important;color:#fff!important}.kyc-reject{background:#b91c1c!important;color:#fff!important}.kyc-actions button:disabled{opacity:.45}.kyc-spinner{padding:40px;text-align:center}.kyc-error{padding:16px;background:#450a0a;color:#fecaca;border-radius:10px}`;document.head.appendChild(style)}
  function clearObjectUrls(){state.objectUrls.forEach(URL.revokeObjectURL);state.objectUrls=[]}function closeModal(){clearObjectUrls();document.getElementById("native-kyc-review-modal")?.remove()}function field(label,value){return `<div class="kyc-review-card"><small>${esc(label)}</small><strong>${esc(value??"—")}</strong></div>`}
  function checksTable(checks){const entries=Object.entries(checks||{});if(!entries.length)return '<p class="kyc-warning">No automated check results were recorded.</p>';return `<table class="kyc-checks"><thead><tr><th>Check</th><th>Status</th><th>Score</th><th>Reasons</th></tr></thead><tbody>${entries.map(([name,row])=>{const status=String(row.status||"unknown");return `<tr><td><strong>${esc(name.replaceAll("_"," "))}</strong><br><small>${esc(row.engine_version||"")}</small></td><td class="kyc-check-${esc(status)}">${esc(status.toUpperCase())}</td><td>${row.score??"—"}</td><td>${(row.reasons||[]).map(esc).join("<br>")||"—"}</td></tr>`}).join("")}</tbody></table>`}
  function assistPanel(a){if(!a)return "";const missing=(a.unavailable_checks||[]).join(", ")||"None";return `<section class="kyc-assist"><h3>AI-assisted Compliance assessment</h3><p>${esc(a.rationale||"")}</p><div class="kyc-assist-grid">${field("Confidence score",`${a.confidence_score??0}%`)}${field("Evidence coverage",`${a.evidence_coverage_score??0}%`)}${field("Recommendation",String(a.recommendation||"").replaceAll("_"," "))}${field("Unavailable checks",missing)}</div><small>Decision support only. The system does not auto-approve or auto-reject; the authorized Compliance officer remains responsible for the final decision.</small></section>`}
  async function loadEvidence(subscriberId,rows,target,button){button.disabled=true;button.textContent="Loading protected evidence…";target.innerHTML="";try{for(const row of rows||[]){const headers={};const t=token();if(t)headers.Authorization=`Bearer ${t}`;const response=await fetch(API_BASE+row.review_url,{credentials:"include",headers,cache:"no-store"});if(!response.ok)throw new Error(`Unable to load ${row.category} (${response.status})`);const blob=await response.blob(),url=URL.createObjectURL(blob);state.objectUrls.push(url);const media=String(row.content_type||"").startsWith("image/")?`<img src="${url}" alt="${esc(row.category)} KYC evidence">`:row.content_type==="application/pdf"?`<iframe src="${url}" title="KYC evidence"></iframe>`:"<p>Preview unavailable.</p>";target.insertAdjacentHTML("beforeend",`<div class="kyc-evidence-item"><strong>${esc(row.category.replaceAll("-"," "))}</strong>${media}</div>`)}button.textContent="Evidence revealed for this review"}catch(error){target.innerHTML=`<p class="kyc-error">${esc(error.message)}</p>`;button.disabled=false;button.textContent="Retry protected evidence"}}
  async function submitDecision(id,decision,reason,attestation,button){if(decision==="REJECTED"&&String(reason||"").trim().length<3){alert("Enter a clear rejection reason.");return}if(!confirm(`${decision==="APPROVED"?"APPROVE":"REJECT"} this Native KYC after human review?`))return;button.disabled=true;try{await reviewRequest(`/admin/kyc/native/${id}/decision`,{method:"POST",body:JSON.stringify({decision,reason:String(reason||"").trim()||null,attestation:!!attestation})});closeModal();if(typeof loadOverview==="function")await loadOverview()}catch(error){alert(error.message);button.disabled=false}}
  async function openReview(id){ensureStyles();closeModal();const b=document.createElement("div");b.id="native-kyc-review-modal";b.className="kyc-modal-backdrop";b.innerHTML='<section class="kyc-modal"><header><div><h2>Native KYC Compliance Review</h2><small>Loading…</small></div><button class="kyc-close">Close</button></header><main><div class="kyc-spinner">Loading KYC review…</div></main></section>';document.body.appendChild(b);b.querySelector(".kyc-close").onclick=closeModal;try{const data=await reviewRequest(`/admin/kyc/native/${id}`),s=data.session||{},i=data.identity||{},r=data.risk||{},u=data.subscriber||{},m=b.querySelector("main");b.querySelector("header small").textContent=`${u.name||"Subscriber"} · ID ${u.id||id} · ${s.reference||""}`;m.innerHTML=`<div class="kyc-warning">Sensitive identity data. Authorized Compliance/Admin review only.</div>${assistPanel(data.assisted_assessment)}<h3>Identity & session</h3><div class="kyc-review-grid">${field("Registered name",u.name)}${field("Email",u.email)}${field("Session status",s.status)}${field("Decision",s.decision)}${field("Date of birth",i.date_of_birth)}${field("Nationality",i.nationality)}${field("Document type",i.document_type)}${field("Issuing country",i.issuing_country)}${field("Document expiry",i.document_expiry)}${field("Document number",i.document_number)}${field("Sanctions status",r.sanctions_status)}${field("AML follow-up",r.aml_followup_required?"Required":"Not required")}${field("Liveness score",r.liveness_score)}${field("Face-match score",r.face_match_score)}</div><h3>Verification checks</h3>${checksTable(data.checks)}<section class="kyc-evidence-toolbar"><button class="kyc-review-button" id="kyc-reveal-evidence">Reveal protected passport / ID and selfie</button></section><div class="kyc-evidence-grid" id="kyc-evidence-grid"></div><section class="kyc-decision"><h3>Compliance decision</h3><textarea id="kyc-review-note" maxlength="500" placeholder="Record the basis for the decision."></textarea><label class="kyc-attest"><input id="kyc-review-attestation" type="checkbox"><span>I confirm that I personally reviewed the evidence and automated checks. The assisted score is advisory only.</span></label><div class="kyc-actions"><button class="kyc-approve" id="kyc-approve-reviewed" disabled>Approve after review</button><button class="kyc-reject" id="kyc-reject-reviewed">Reject after review</button></div></section>`;const reveal=m.querySelector("#kyc-reveal-evidence"),att=m.querySelector("#kyc-review-attestation"),approve=m.querySelector("#kyc-approve-reviewed"),reject=m.querySelector("#kyc-reject-reviewed");reveal.onclick=()=>loadEvidence(id,data.evidence||[],m.querySelector("#kyc-evidence-grid"),reveal);att.onchange=()=>approve.disabled=!att.checked;approve.onclick=()=>submitDecision(id,"APPROVED",m.querySelector("#kyc-review-note").value,att.checked,approve);reject.onclick=()=>submitDecision(id,"REJECTED",m.querySelector("#kyc-review-note").value,att.checked,reject)}catch(error){b.querySelector("main").innerHTML=`<p class="kyc-error">${esc(error.message)}</p>`}}
  function securePendingKycActions(){const table=document.querySelector("#subscribers-table");if(!table)return;table.querySelectorAll('button[data-action="kyc-approve"]').forEach(approve=>{const id=approve.dataset.id,reject=table.querySelector(`button[data-action="kyc-reject"][data-id="${CSS.escape(id)}"]`);if(approve.disabled)return;approve.disabled=true;if(reject)reject.disabled=true;const actions=approve.parentElement;if(!actions||actions.querySelector(`[data-native-kyc-review="${CSS.escape(id)}"]`))return;const review=document.createElement("button");review.type="button";review.className="kyc-review-button";review.dataset.nativeKycReview=id;review.textContent="Review KYC";review.onclick=()=>openReview(Number(id));actions.insertBefore(review,approve)})}
  function start(){ensureStyles();securePendingKycActions();const table=document.querySelector("#subscribers-table");if(table)new MutationObserver(securePendingKycActions).observe(table,{childList:true,subtree:true})}if(document.readyState==="loading")document.addEventListener("DOMContentLoaded",start);else start();
})();
